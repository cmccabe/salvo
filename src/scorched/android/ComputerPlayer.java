package scorched.android;

public class ComputerPlayer extends Player {

    /*================= Lifecycle =================*/
    ComputerPlayer(int id, String name, Player.PlayerColor color) {
        super(id, name, color);
    }
}
